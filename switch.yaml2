switch:
  - platform: template
    name: "Ignore doors / mirrors / pet"
    id: ignore_static_reflections
    optimistic: true
    restore_state: true
    entity_category: config

  - platform: template
    name: "Shower mode"
    id: shower_mode
    optimistic: true
    restore_state: true
    entity_category: config
